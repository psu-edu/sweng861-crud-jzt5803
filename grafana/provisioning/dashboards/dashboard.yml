# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: 'Campus Analytics'
    orgId: 1
    folder: 'Campus Analytics'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
